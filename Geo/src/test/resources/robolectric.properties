# The app's targetSdk is 36, which Robolectric 4.14 cannot emulate
# (it supports up to API 35). Without this, Robolectric-backed unit tests
# fail to configure with "targetSdkVersion=36 > maxSdkVersion=35".
# Pin the unit-test emulated SDK to a supported level.
sdk=34
